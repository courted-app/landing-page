[build]
  command = "npm run build"
  publish = "dist"

# Disable secrets scanning smart detection to avoid false positives
# The Google Places API key is a public client-side key, not a secret
[build.environment]
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"

